# spring.application.name=travaux
# # Use 'db' because that is the service name in docker-compose.yml
# spring.datasource.url=jdbc:postgresql://db:5432/signalement_db
# spring.datasource.username=user
# spring.datasource.password=password
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# server.port=8085
# server.address=0.0.0.0

spring.application.name=travaux
# Use 'db' because that is the service name in docker-compose.yml
spring.datasource.url=jdbc:postgresql://localhost:5432/signalement_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
# =======
# spring.application.name=authentification
# server.port=8081
# # Use 'db' because that is the service name in docker-compose.yml
# spring.datasource.url=jdbc:postgresql://localhost:5432/signalement_db
# spring.datasource.username=postgres
# spring.datasource.password=RiaNa004
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true
# server.port=8085
# server.address=0.0.0.0
